# دليل موديول الموارد البشرية والرواتب (HR & Payroll Module Guide)

أهلاً بك! يوضح هذا المستند تفصيلياً كل ما تم بناءه وتطويره في **موديول الموارد البشرية والرواتب والبصمة (HR & Payroll Module)** لنظام **Alex Copier ERP**، بالإضافة إلى **دليل شامل للاختبار الميداني (Step-by-Step Testing Guide)**.

---

## 1. ما تم إنجازه وتطويره (Overview of What Was Built)

### أ. واجهات المستخدم والسايد بار (Frontend UI & Navigation)
تمت إضافة مجموعة **الموارد البشرية** كاملة إلى القائمة الجانبية (Sidebar) مع التحكم في الصلاحيات لظهورها للأدوار المصرح لها فقط (مثل المدير العام، مدير الشركة، ومدير الموارد البشرية):
1. **لوحة تحكم الموارد البشرية (`/hr`)**:
   - إحصائيات سريعة: إجمالي الموظفين، الحاضرون اليوم، طلبات الإجازة المعلقة، الغائبون المتأخرون.
   - كروت تنقل سريع إلى كافة شاشات النظام.
2. **إدارة الموظفين (`/hr/employees`)**:
   - إضافة موظف جديد (تدعم زر الإضافة السريع `+` من السايد بار وعبر الرابط `?add=1`).
   - تخصيص كود الموظف، الرقم القومي، رقم البصمة (Fingerprint ID)، الراتب الأساسي، الشركة، القسم، والمسمى الوظيفي.
   - تعديل بيانات الموظف، استعراض الموظف، وحذف الموظف.
   - فلترة حسب القسم وحالة الموظف (نشط، إجازة، موقوف، منهى الخدمة).
3. **التحضير والبصمة اليومية (`/hr/attendance`)**:
   - عرض سجلات الحضور والبصمة لكل يوم مع تحديد وقت الدخول، وقت الخروج، ساعات العمل، ومحاسبة التأخير بالدقائق.
   - تسجيل حضور وبصمة يدوية للموظفين (Manual Punch) عند الأعطال أو الاستئذانات.
   - فلترة حسب التاريخ والاسم وحالة الحضور (حاضر، متأخر، غائب، إجازة).
4. **الإجازات والاستئذانات (`/hr/leaves`)**:
   - تقديم طلب إجازة (سنوية، مرضية، عارضة، بدون أجر، أمومة).
   - مراجعة الطلبات بواسطة الإدارة والموافقة أو الرفض مع كتابة سبب الرفض.
   - الخصم التلقائي لعدد الأيام من رصيد إجازات الموظف عند الموافقة.
5. **الرواتب ومسيرات الرواتب (`/hr/payroll`)**:
   - احتساب مسير الرواتب الشهري أوتوماتيكياً بزر واحد بناءً على أيام العمل والبصمات والخصومات والسلف والتأمينات.
   - اعتماد الكشف المالي (Approved) وقفل وصرف الرواتب (Locked).
   - استعراض مفردات راتب كل موظف (مفردات مرتب) وتصدير الكشوفات.
6. **الأقسام والمسميات الوظيفية (`/hr/departments`)**:
   - إضافة وإدارة أقسام الشركة والمسميات الوظيفية وهيكلة الكوادر.

---

## 2. المحركات الخلفية وقواعد البيانات (Backend Engines & DB)

1. **قواعد البيانات (Prisma Schema)**:
   - نماذج الموظفين `Employee` والأقسام `Department` والمسميات `JobTitle`.
   - سجلات البصمة الخام `RawAttendancePunch` وسجلات الحضور اليومي `DailyAttendanceRecord`.
   - طلبات الإجازة `LeaveRequest` وتتبع الأرصدة السنوية والمرضية والعارضة.
   - مسيرات الرواتب `PayrollRun` ومفردات الرواتب `PayrollItem` والسُلف `EmployeeAdvance` والقروض `LoanInstallment`.
2. **محركات محاكاة واحتساب الموارد البشرية**:
   - **محرك الحضور والتأخير (`attendance-engine.ts`)**: يحسب ساعات العمل، التأخير بالدقائق، الخروج المبكر، وحساب الإضافي.
   - **محرك احتساب الرواتب (`payroll-engine.ts`)**: يحسب الإجمالي (Gross) والصافي (Net) والبدلات والخصومات والسلف والقروض والضرائب والتأمينات.
   - **محرك الإجازات (`leave-engine.ts`)**: يتحقق من عدم تداخل الإجازات ويخصم من الرصيد المتاح.
   - **نظام الإشعارات (`hr-notifications.ts`)**: يرسل تنبيهات فورية للموظف والإدارة عند تقديم أو قبول/رفض الإجازات والرواتب.

---

## 3. دليل الاختبار الميداني خطوة بخطوة (Step-by-Step Test Guide)

يمكنك تجربة الموديول بالكامل على النظام باتباع الخطوات التالية:

### الخطوة 1: اختبار الأقسام والمسميات الوظيفية
1. افتح السايد بار واضغط على قسم **الموارد البشرية** -> **الأقسام والمسميات**.
2. اضغط على **إضافة قسم**:
   - أدخل كود القسم: `ENG`
   - اسم القسم: `الصيانة والخدمة الفنية`
   - اضغط **حفظ القسم**.
3. اضغط على **إضافة مسمى وظيفي**:
   - الكود: `SR-ENG`
   - المسمى الوظيفي: `مهندس صيانة ممتاز`
   - اختر القسم: `الصيانة والخدمة الفنية`
   - اضغط **حفظ المسمى**.

---

### الخطوة 2: اختبار إضافة موظف جديد
1. من السايد بار، اضغط على زر `+` بجانب **الموظفون** أو افتح شاشة **الموظفون** واضغط **إضافة موظف**.
2. أدخل البيانات التالية:
   - كود الموظف: `EMP-001`
   - رقم البصمة: `101`
   - الاسم الكامل: `أحمد محمد علي`
   - رقم الهاتف: `01012345678`
   - تاريخ التعيين: `2026-01-01`
   - الراتب الأساسي: `6000`
   - القسم: `الصيانة والخدمة الفنية`
3. اضغط **إضافة الموظف**.
4. تأكد من ظهور الموظف في الجدول مع رقم البصمة والراتب والبيانات كاملة.

---

### الخطوة 3: اختبار سجل الحضور والبصمة اليومية
1. انتقل إلى شاشة **التحضير والبصمة** (`/hr/attendance`).
2. اضغط على **تسجيل حضور يدوي**:
   - اختر الموظف: `EMP-001 - أحمد محمد علي`
   - التاريخ: تاريخ اليوم
   - وقت الدخول: `09:00`
   - وقت الخروج: `17:00`
   - الحالة: `حاضر (Present)`
   - اضغط **حفظ السجل**.
3. ستلاحظ ظهور السجل فوراً موضحاً عدد ساعات العمل (8 ساعات) وحالة الحضور باللون الأخضر.

---

### الخطوة 4: اختبار طلبات الإجازات والاستئذانات
1. انتقل إلى شاشة **الإجازات والاستئذانات** (`/hr/leaves`).
2. اضغط على **طلب إجازة جديد**:
   - اختر الموظف: `أحمد محمد علي`
   - نوع الإجازة: `سنوية (Annual)`
   - من تاريخ إلى تاريخ (مثلاً 3 أيام).
   - السبب: `إجازة شخصية`
   - اضغط **تقديم الطلب**.
3. سيظهر الطلب بحالة **معلقة (Pending)** وبجانبه زران (موافقة / رفض).
4. اضغط على **موافقة**، وستتحدث حالة الطلب إلى **مقبول (Approved)** وسيخصم تلقائياً من رصيد الإجازات.

---

### الخطوة 5: احتساب مسير الرواتب الشهرية وقفله
1. انتقل إلى شاشة **الرواتب والمسيرات** (`/hr/payroll`).
2. اضغط على **احتساب مسير رواتب جديد**:
   - اختر الشهر والسنة الحاليين.
   - اضغط **بدء الاحتساب**.
3. سيقوم النظام بحساب الرواتب تلقائياً وعرض المسير بحالة **محسوب (Draft)** مع تفاصيل إجمالي الرواتب والخصومات والصافي.
4. اضغط على زر **العين (View)** لمشاهدة كشف مفردات المرتب لكل موظف.
5. اضغط على زر **اعتماد** للرواتب، ثم **قفل وصرف** لإغلاق الشهر وتأكيد الصرف.

---

## 4. اختبارات الأكواد والوحدات التلقائية (Automated Unit Tests)
تم فحص الكود والتأكد من نجاح كافة الاختبارات التلقائية لجميع محركات HR والـ Vitest test suite بنجاح 100%:
- `src/lib/hr/attendance-engine.test.ts`
- `src/lib/hr/payroll-engine.test.ts`
- `src/lib/hr/leave-engine.test.ts`

يمكنك تشغيل الاختبارات في أي وقت عبر الأمر:
```bash
npm test
```
