# NEEDS_DECISION — أسئلة محتاجة رد بشري قبل/أثناء التنفيذ

> **أي بند هنا ❌ ممنوع أنفّذه قبل ما حد بشري يرد ويكتب الإجابة في عمود «القرار».**
> البنود دي فيها **قرار بيزنس** أو **معلومة مش متوفرة** — مش مجرد كود.
> **شرط الدخول لـ P2/P3:** P0 و P1 ✅ كلهم + بنود P1 المعمارية (2.10 / 2.11 / 2.20) اتراجعت بشري.

---

## A. قرارات بيزنس (تحتاج owner من الإدارة/المالية)

### A1 — ❓ بند 4.3: هل الفوترة المفروض تكون بعدد النسخ؟
- **الحالة:** من الـ **1,664** عقد، **صفر** عقد بيفوتّر بعدد النسخ (`costPerCopy = 0`).
- **اللي محتاج قرار:** العقود بتسعّد بسعر ثابت ولا بيشتغلوا على عدّاد المنسوخات؟
- **ليه مهم:** لو الإجابة «بعدد النسخ»، يبقى لازم (أ) نبدأ نكتب في `MeterReading` (بند 3.1)، (ب) نحط سعر نسخ في العقد، (ج) نعمل تقرير التسوية. ده تغيير في نموذج الدخل كله.
- **الإجابة:** _(فاضية)_

### A2 — ❓ بند 2.5: إيه رقم «صافي الربح» الصح؟
- **الحالة:** في رقمين مختلفين — `companies/route.ts:56` و `companies/[id]/report/route.ts:262`.
- **اللي محتاج قرار:** التقرير بيحسب `+ totalSettlements` كأنها دخل. **هل التحصيل يدخل دخل ولا مجرد تحصيل دين؟** (محاسبيًا التحصيل بيلغي دين مش بيولّد ربح.)
- **الخيارات:**
  - (أ) `netProfit = totalSales - totalPurchases - totalExpenses - salesReturns + purchaseReturns` ← اللي التقرير باقترحه
  - (ب) تسيب الرقمين زي ما هما بس تسمّيهم مختلفين (`netProfit` مقابل `operatingCashflow`)
- **ليه مهم:** التقرير الحالي **بيتوزع على المستثمرين** — رقم غلط = توزيعات غلط.
- **الإجابة:** _(فاضية)_

### A3 — ❓ بند 2.19: التحصيل مربوط لشركة واحدة بالاسم `"اليكس كوبير"`
- **الحالة:** `customers/[id]/tests/route.ts:226-228` بيدوّر على شركة بالاسم الثابت.
- **اللي محتاج قرار:** هل النظام في فعلاً شركة واحدة بس، ولا فيه شركات تانية دخلت؟ (الجداول فيها `companyId` بالفعل.)
- **الإجابة:** _(فاضية)_

### A4 — ❓ بند 2.12: منع التعديل اليدوي لمبالغ العميل
- **الحالة:** الـ UI بيكتب `Math.max(0, remainingDebt)` — يعني **الدين تحت الحساب بيتضيّع**، والرصيد بيبقى في 3 أماكن مختلفة.
- **اللي محتاج قرار:** هل المحاسبة بتعدّل `remainingDebt` يدويًا من شاشة تعديل العميل؟ لو أيوه، فين بالظبط وإزاي؟ (المقترح: نمنع الحقل من التعديل ونخلي المبالغ محسوبة آليًا + تقرير «فروقات الرصيد».)
- **الإجابة:** _(فاضية)_

### A5 — ❓ بند 1.6: مدة صلاحية رابط كشف الحساب
- **الحالة:** التوكن **ما بينتهيش أبدًا** (مفيش أصلاً `statementTokenExpiresAt` في القاعدة).
- **اللي محتاج قرار:** المقترح 30 يوم. بس الروابط قد تكون متنسخة عند عملاء — **كام يوم مناسب؟** وهل نبعث إشعار قبل الانتهاء؟
- **الإجابة:** _(فاضية)_

### A6 — ❓ بند 1.5: مسح الدفاتر في الإنتاج
- **الحالة:** `/api/dev/reset-transactions` و `/api/companies/[id]/reset-transactions` بيعملوا `deleteMany({})` على كل الحركات المالية.
- **اللي محتاج قرار:** هل الميزة مطلوبة في الإنتاج أصلاً؟ المقترح: **نمنعها تمامًا في الإنتاج** ورا `ENABLE_DATA_RESET=1` (والشرط موجود في تقرير المراجعة).
- **الإجابة:** _(فاضية)_

---

## B. مخططات قاعدة بيانات (لا يمكن تخمينها — لازم snapshot + مراجعة بشرية)

### B1 — ⛔ بند 2.10: ربط التسويات بمصدرها (الـ ١.٥٩ مليون)
- **الحالة:** `Settlement` فيها `customerId / engineerId` بس، مفيش `copierTestId` ولا `salesOrderId` ولا `returnTransactionId`. النظام بيفرّق بينهم بالبحث في نص `reason`.
- **المطلوب:** موافقة على الـ migration (إضافة 4 أعمدة + 3 فهارس + 2 relations).
- **سؤال مهم للإرجاع (backfill):** التسويات الـ **1,156** الموجودة (1,590,342 ج.م) — نعمل لها backfill تلقائي parse من `reason`، **ولا** نخليها `sourceType = "MANUAL"` ونعتبرها «غير قابلة للتتبع»؟
  - **اقتراح:** backfill بـ parse، وأي تسوية مش ليها parse تطابق تتسجل في تقرير مراجعة يدوي (مش نلفط).
- **الإجابة:** _(فاضية)_

### B2 — ⛔ بند 2.11: تحويل الفلوس من `Float` لـ `Decimal(18,2)`
- **الحالة:** ~90 حقل فلوس `Float` → `double precision` في Postgres. ده سبب عدم تطابق الرصيد.
- **اللي محتاجه:**
  1. **snapshot كامل** من قاعدة الإنتاج قبل التنفيذ.
  2. مراجعة بشرية لاختيار الأعمدة (كلهم؟ ولا الـ settlement/customer/ledger/journal بس؟).
  3. تحديد مين هينفّذ الـ migration على الإنتاج، وإمتى (في نافذة صيانة؟).
- **الإجابة:** _(فاضية)_

### B3 — ⛔ بند 2.20: قيود `CHECK` على مستوى القاعدة
- **الحالة:** صفر `CHECK` constraints. المقترح 4 (تواريخ العقد، تواريخ الإجازة، مبلغ التسوية > 0، طرف القيد الواحد).
- **الخطر:** لو في بيانات مخالفة بالفعل في الإنتاج، الـ migration هيفشل. محتاجين **فحص بيانات أولًا** على نسخة.
- **الإجابة:** _(فاضية)_

---

## C. قرارات تقنية (مش بيزنس بس محتاج تأكيد)

### C1 — ❓ بند 3.20 / 1.2: مفتاح `hrSelfService` ممنوح لكل الأدوار الـ9 بس مفيش له صفحة
- **الحالة:** `permissions.ts:44` بيمنح `hrSelfService` + `hrReports` لكل الأدوار، بس مفيش صفحة ولا سايد بار ليهم.
- **أثر بعد بند 1.2 (✅ اتعمل):** مفيش حد غير أصحاب صفحات HR يقدر يقرأ/يكتب في `/api/hr/*` — يعني **العامل العادي مش قادر يقدّم طلب إجازة بنفسه** (POST `/api/hr/leaves` بقى `hrLeaves` بس).
- **الخيارات:** (أ) نبني صفحة «خدمتي الذاتية» (رواتي، إجازاتي، حضوري) وبنسمح للـ POST بـ `hrSelfService` لما يكون الطلب لنفسه بس · (ب) نستنى ونسيب السلوك الحالي (الموظف لازم يروح لمدير HR).
- **الإجابة:** _(فاضية)_

### C2 — ❓ بند 2.3: سلوك «كشف الرواتب اتحسب قبل كده»
- **الحالة:** المقترح يرجّع 409 `PAYROLL_ALREADY_CALCULATED` بدل ما يحسب مرتين.
- **اللي محتاج قرار:** هل في حاجة في الواجهة محتاجة تفضل تشتغل؟ يعني: لو الشهر اتحسب و حد عدّل الداتا بعد كده، إيه السلوك المطلوب؟)
- **الإجابة:** _(فاضية)_

### C3 — ❓ بند 2.13: حذف الماكينة
- **الحالة:** المقترح يمنع الحذف لو الماكينة ليها سجلات (صيانة/عدادات/ضمان/عقد/اختبارات) ويقول «امسحها كخردة».
- **اللي محتاج قرار:** ولا فيه حالة لازم تفضل قابلة للحذف (مثل ماكينة اتسجلت غلط)؟ لو أيوه: مين يقدر يعمل الاستثناء وبأي شرط؟
- **الإجابة:** _(فاضية)_

### C4 — ❓ بند 1.4: تجاوز حماية `seed.ts`
- **الحالة:** اتنفّذ ✅ — يمنع التشغيل إلا على localhost إلا مع `ALLOW_DESTRUCTIVE_SEED=1`.
- **اللي محتاج قرار:** تأكيد إن مفيش بيئة staging/مؤقتة محتاجة الـ seed بـ TRUNCATE على قاعدة مش localhost.
- **الإجابة:** _(فاضية)_

### C5 — ⛔ بند 1.6: الروابط اللي اتبعتت للعملاء هتبطل تشتغل
- **الحالة:** اتنفّذ الكود ✅. التوكن بقى بينتهيش بعد 30 يوم، والمسار العام بيرجّع 410 `TOKEN_EXPIRED`. العميل اللي مالهوش تاريخ انتهاء (كل الروابط القديمة) بيتحسب **منتهٍ** — وده مقصود أمنيًا.
- **migration مكتوبة ومش متشغّلة:** `prisma/migrations/20260925000000_statement_token_expiry/migration.sql` (عمودين على `Customer` وعمودين على `Engineer`).
- **⚠️ لازم قرار بشري قبل النشر:**
  1. مين هينفّذ الـ migration على الإنتاج، وإمتى.
  2. **كل رابط كشف حساب اتبعت لعميل أو مهندس لحد دلوقتي هيطلّع 410** — هل نبلغ العملاء بإعادة الإصدار (زرار في الصفحة)، ولا نعمل migration بتبعت تاريخ انتهاء بعيد لكل الروابط الموجودة (سيكيوريتي أضعف بس بتفادي إزعاج)؟
  3. مدة الـ 30 يوم مناسبة؟ ولا تفضّل مدة تانية (أقصر للأمان، أطول للراحة)؟
- **الإجابة:** _(فاضية)_

### C6 — ❓ بند 1.7: صفحات اتمنحت للأدوار + self-scoping
- **الحالة:** اتنفّذ ✅ — 42 مسار GET اتقفلوا بصفحة كل واحد.
- **قرارات اتاخدت بإجابتك (بند 1.7):** كل مسار اتربط بصفحته، و`ROLE_PERMISSIONS` اتزودت بـ `companies`+`investors` لـ`COMPANY_MANAGER` و`engineers` لـ`ENGINEER`.
- **⚠️ leftovers محتاجة تأكيدك:**
  1. `dashboard` و`notifications` اتربطوا بصفحة `dashboard` — وده **كل الأدوار الـ9 عندها**، فعمليًا مش تغيير. لو عايز `notifications` تبقى مقفولة على أدوار معيّنة، قول.
  2. `expense-categories` → `finance` و`sales-categories` → `sales` — تصنيفات إعداد، مش معروف هل المفروض تبان في `settings`. قول لو عايزهم على `settings`.
  3. `engineers/[id]/statement` و`engineers/[id]/sales` بقوا **self-scoped**: المهندس يقرأ نفسه بس، والبقية محتاجين `engineers`/`sales`. ده **أضيق** من طلبك (طلبت منح `ENGINEER` صفحات) — السبب إن منحه `sales` كان هيفتح له كل المبيعات مش بس مبيعاته. لو عايزه يقرأ مبيعات أي مهندس، قول.
- **الإجابة:** _(فاضية)_

---

## D. بنود اتخطّت في التنفيذ (لازم السبب يكون مكتوب هنا)
_(فاضي — املأه أول ما تتخطّى بند)_

| البند | التاريخ | السبب | البديل |
|---|---|---|---|
| — | — | — | — |
