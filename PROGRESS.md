# PROGRESS — تنفيذ تقرير `CODE-AUDIT.md`

> **اقرأ الملف ده أول حاجة في أي جلسة جديدة (بما فيهم جلسة الـ AI اللي بتكمّل).**
> الترتيب الإجباري: **P0 كامل** ← ثم **P1** ← ثم **P2** ← ثم **P3**.
> ممنوع تتخطّى بند إلا لو فيه قرار بيزنس أو معلومة ناقصة — وسجّل السبب هنا.

## القواعد الصارمة

1. **بند واحد = كوميت واحد = push واحد.** ممنوع تجميع بنود في كوميت.
2. **ممنوع لمس قاعدة الإنتاج.** أي migration تُكتب كملف فقط ومتشغّلهاش تلقائي.
3. لو الاختبارات (259) مش شاملة البند، **زوّد اختبار** يغطيه (خصوصًا double-click / race condition).
4. كل بند لازم يعدّي: `npm test` + `npm run lint` + `npm run build`.
5. لو P0 و P1 خلصوا، **لازم حد بشري يراجع** بنود `NEEDS_DECISION.md` قبل ما تبدأ P2/P3.
6. في آخر كل جلسة: اكتب قسم «آخر حالة» في آخر الملف → كوميت + push.

## حالة التنفيذ

Legend: ⬜ لسه · 🟡 شغال · ✅ خلص · ⏭️ متخطّى (لازم سبب)

### P0 — أمان

| # | البند | الحالة | كوميت | ملاحظات |
|---|---|---|---|---|
| 1.1 | `GET /api/returns/[id]` مفتوح بدون تسجيل دخول | ✅ | `?` | `requireAuth` (401) + `requirePageAccess("returns")` (403) + 5 اختبارات جديدة |
| 1.2 | موديول HR كله بدون صلاحيات | ✅ | `?` | 16 handler في 7 ملفات: `requirePageAccess` بدل `requireAuth` وحده. **اتوسّعنا للقراءات كمان** مش الكتابة بس — نفس الثغرة بالظبط. 35 اختبار جديد |
| 1.3 | XSS مخزّن في فواتير/سندات الطباعة | ✅ | `?` | `src/lib/html-escape.ts` (دالة `esc` واحدة) + تهريب **كل** قيمة مستخدم في `invoice-template.ts` (فاتورة + ريسيت 58/80) و `supplier-statement.ts` + `X-Content-Type-Options: nosniff`. 18 اختبار جديد |
| 1.4 | `seed.ts` بيمسح قاعدة الإنتاج | ✅ | `?` | `prisma/destructive-guard.ts` — يرفض التشغيل قبل أي اتصال، إلا لو localhost أو `ALLOW_DESTRUCTIVE_SEED=1`. الـ host بيتفكّع بـ `URL` مش substring (عشان `localhost.evil.com` ما يعدّيش). 19 اختبار. **اتأكدت يدويًا:** رابط neon بيدي رفض، و localhost بيمر |
| 1.5 | مسح الدفاتر متاح في الإنتاج | ✅ | `?` | `/api/dev/reset-transactions` و `/api/companies/[id]/reset-transactions` بقى 404 `DISABLED` في الإنتاج إلا بـ `ENABLE_DATA_RESET=1`. 9 اختبارات جديدة (2 منها بتبقّي شغالة خارج الإنتاج) |
| 1.6 | رابط كشف الحساب ما بينتهيش أبدًا | ✅ | `?` | التوكن بيتجدّد مع كل ضغطة (زرار الإلغاء) + صلاحية 30 يوم. **كمّلت على المهندسين كمان** — نفس الكود حرفيًا ونفس الثغرة، والتقرير ما ذكرش غير العملاء. المسار العام بيرجّع 410 `TOKEN_EXPIRED` ولو التوكن مالهوش تاريخ انتهاء (يعني الروابط القديمة هتطير). **migration مكتوبة بس مش متشغّلة** — شوف C5 |
| 1.7 | كل قراءات الـ GET مفتوحة لأي مستخدم | ✅ | `?` | **42 مسار GET** (التقرير قدّر ~50) اتقفلوا بصفحة الـ `requirePageAccess`/`requireAnyPage` الجديدة في `auth-helpers.ts`، مع 401/403 مفصولين. **زيادتين في `ROLE_PERMISSIONS` عشان متقفلش حاجة على حد شغال:** `COMPANY_MANAGER` كسب `companies`+`investors`، و`ENGINEER` كسب `engineers`. ولأن `engineers/[id]/statement` و`/sales` كانا مفتوحين لأي حد، بقوا **self-scoped** — المهندس يقرأ نفسه بس. 3 ملفات اختبار (116 اختبار). اتأكدت إن مفيش GET فاضل على `requireAuth` وحدها |

### P1 — فلوس وتطابق بيانات

| # | البند | الحالة | كوميت | ملاحظات |
|---|---|---|---|---|
| 2.1 | الرواتب بتقع (enum `PayrollItemFlag` ناقص) | ⬜ | — | |
| 2.2 | قفل الرواتب بيقفل الشهر الغلط | ⬜ | — | |
| 2.3 | حساب كشف الرواتب مش idempotent | ⬜ | — | |
| 2.4 | تكلفة البيع بين الشركات من عند البائع | ⬜ | — | |
| 2.5 | رقمان مختلفان لصافي الربح | ⬜ | — | |
| 2.6 | تسوية معتمدة ترجع INITIAL وتتحقق مرتين | ⬜ | — | |
| 2.7 | دفع الأقساط مرتين (double-click) | ⬜ | — | |
| 2.8 | فقد تحديث في رصيد العميل | ⬜ | — | |
| 2.9 | الاختبار + التسوية مش في transaction واحدة | ⬜ | — | |
| 2.10 | مفيش رابط بين التسوية والمصدر (١.٥٩ مليون) | ⬜ | — | |
| 2.11 | الفلوس متخزنة `Float` | ⬜ | — | |
| 2.12 | ثلاث مصادر للرصيد الواحد | ⬜ | — | |
| 2.13 | حذف الماكينة بيمسح سجلها كله بصمت | ⬜ | — | |
| 2.14 | حذف أمر شراء مستلَم بيسيب المخزون زائد | ⬜ | — | |
| 2.15 | الإجازات: `Math.abs` بيقبل تاريخ معكوس | ⬜ | — | |
| 2.16 | الماكينة يقدر يكون في حالتين في نفس الوقت | ⬜ | — | |
| 2.17 | طلبات الشراء: الـ body كله لـ Prisma | ⬜ | — | |
| 2.18 | أرقام المستندات `Date.now()` (تصادم) | ⬜ | — | |
| 2.19 | التحصيل مقيّد في شركة واحدة بالاسم | ⬜ | — | |
| 2.20 | مفيش قيود CHECK على مستوى القاعدة | ⬜ | — | |

### P2 — ربط الفرونت بالباك + أداء

| # | البند | الحالة | كوميت | ملاحظات |
|---|---|---|---|---|
| 3.1 | `MeterReading` مفيش حاجة بتكتب فيه | ⬜ | — | |
| 3.2 | البحث مش متوافق مع العربي | ⬜ | — | |
| 3.3 | قاعدة CSS بتلغي عرض الجداول | ⬜ | — | |
| 3.4 | ضغفت fetch على كل عملية تعديل | ⬜ | — | |
| 3.5 | استعلامات مفتوحة على جداول ضخمة | ⬜ | — | |
| 3.6 | فهارس ناقصة (11 `@index` في 71 موديل) | ⬜ | — | |
| 3.7 | مفيش `take` في تحميل العملاء | ⬜ | — | |
| 3.8 | عناوين الصفحات غلط في 15 صفحة | ⬜ | — | |
| 3.9 | الداشبورد مثبّت RTL حتى لو المستخدم إنجليزي | ⬜ | — | |
| 3.10 | `toISOString()` بيسجّل اليوم غلأ | ⬜ | — | |
| 3.11 | عربية مكتوبة في كومبوننتات مشتركة | ⬜ | — | |
| 3.12 | مودال النظام كله ناقص وصولية | ⬜ | — | |
| 3.13 | 25 صفحة بتنادي `filtered` من غير `useMemo` | ⬜ | — | |
| 3.14 | 17 صفحة البحث فيها مش بيرجع لصفحة 1 | ⬜ | — | |
| 3.15 | صفحات بتتجاهل رد السيرفر | ⬜ | — | |
| 3.16 | بولينج كل 30 ثانية في الهيدر | ⬜ | — | |
| 3.17 | أرقام عربية-هندية في كشف الحساب | ⬜ | — | |
| 3.18 | مفيش CSS للطباعة في كشوف الحساب | ⬜ | — | |
| 3.19 | تكرار في الكود | ⬜ | — | |
| 3.20 | مفاتيح صلاحيات بلا صفحة | ⬜ | — | |
| 3.21 | الـ lint فيه 73 error قديم | ⬜ | — | |
| 3.22 | ملاحظات (`seed-access.ts` في مسار الإنتاج) | ⬜ | — | |

### P3 — فيتشر وتقارير

> ⛔ **ممنوع تبدأ P3 إلا بعد:** (أ) خلاص P0 و P1 كلهم ✅، و(ب) **مراجعة بشرية** لبند `NEEDS_DECISION.md`.

| # | البند | الحالة | كوميت | ملاحظات |
|---|---|---|---|---|
| 4.1 | تقرير «أعمار الديون» | ⬜ | — | |
| 4.2 | شاشة تسويات بانتظار التحقق + دفعة جماعية | ⬜ | — | |
| 4.3 | توصيل العدادات بـ `MeterReading` + تقرير | ⬜ | — | فيه قرار بيزنس |
| 4.4 | تقرير «العقود المستحقة للتجديد» | ⬜ | — | |
| 4.5 | عمود «حالة التحصيل» في صفحة الاختبارات | ⬜ | — | |
| 4.6 | تقرير إنتاجية المهندس بالتحصيل | ⬜ | — | |
| 4.7 | رسوم بيانية (sparkline / bar chart) | ⬜ | — | |

## آخر حالة

_(يتكتب هنا في آخر كل جلسة)_

- **آخر بند تم بنجاح:** **1.7** — وده ** آخر بند في P0**. يعني P0 خلص بالكامل (1.1 → 1.7) والكل مدفوع على `new/main`.
- **حاجة نص مخلصة:** لا شيء — مفيش تعديل في الشجرة دلوقتي غير ملفات الـ tracking اللي هتترفع مع الكوميت ده.
- **التحقق:** `npm test` = 36 ملف / 483 اختبار، كلهم ناجحين. `npm run build` = Compiled. `npm run lint` = 166 مشكلة (70 error + 96 warning) — **دي نفس الـ baseline القديمة**، مفيش errors جديدة من شغلنا.
- **الخطوة الجاية:** ابدأ **P1 — بند 2.1** (الرواتب بتقع 500 — enum `PayrollItemFlag` ناقص). اقرأ `CODE-AUDIT.md` section 2.1 (سطر ~165) قبل ما تعدّل.
- **حاجات معلّقة على قرار بشري (مهمة جدًا):**
  - **C5** — بند 1.6 كتب migration (`20260925000000_statement_token_expiry`) **ومش متشغّلة على الإنتاج**. لازم حد ينفّذها، ولازم قرار: الروابط القديمة هتبطل (410) — نبلّغ العملاء ولا نمدّدها؟
  - **C6** — بند 1.7: تأكيدات على `dashboard`/`notifications` والتصنيفات، وعلى إن مبيعات المهندس بقت self-scoped.
  - **C1** — بند 1.2: العامل العادي مش قادر يقدّم إجازة بنفسه (لسه).
  - **B1/B2/B3** — قرارات P1 (backfill التسويات، Float→Decimal، قيود CHECK).
- **ملاحظات للـ AI اللي هيكمل:** اقرأ `PROGRESS.md` و`NEEDS_DECISION.md` الأول. ابدأ من أول ⬜ في **P1** بالترتيب. راجع `CODE-AUDIT.md` للبند نفسه قبل ما تعدّل. **P2/P3 ما تبدأش فيها لحد ما P1 يخلص والمراجعة البشرية تعدّي.** الـ remote الحي هو `new` (مش `origin`).
